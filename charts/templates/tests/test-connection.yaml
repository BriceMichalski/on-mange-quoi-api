apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "charts.fullname" . }}-test-connection"
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "charts.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
